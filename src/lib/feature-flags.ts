/** Inbox nav and dashboard widgets — set true when ready to ship. */
export const INBOX_NAV_ENABLED = false;

/** Partner detail sentiment monitor section — set true when ready to ship. */
export const SENTIMENT_MONITOR_ENABLED = false;

/** Moss enterprise insights — off until overseas Moss data is useful. */
export const MOSS_ENABLED = false;
